const PRIMARY = "primary";
const GHOST = "ghost";
const DASHED = "dashed";
const LINK = "link";
const TEXT = "text";
const DEFAULT = "default";

const BUTTON_TYPES = {
  PRIMARY,
  GHOST,
  DASHED,
  LINK,
  TEXT,
  DEFAULT,
};

export default BUTTON_TYPES;
